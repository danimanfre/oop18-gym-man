package gymman.ui;

/**
 * The Controller interface. Every Controller in the app should implement it
 */
public interface Controller {
    /**
     * Called when the corresponding page is displayed, for example by navigating to it
     */
    void onDisplay();
}
